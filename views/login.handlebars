<h2>Login</h2>

{{#if error}}
    <h4>Something went wrong. Please enter both your email-address and password.</h4>
{{/if}}

{{#if errorpassword}}
    <h4>Your password does not match your email-address. Please try again.</h4>
{{/if}}

short context info about petition<br>
Register for a world without algorithmic bias<br>
Every vote supports the cause for a fair and just ...<br>

<form method="POST" id="loginfields">
    <div id="twofields">
        <input id="email-login" name="email" placeholder="enter email address" type="text" autocomplete="off" />
        <input id="password-login" name="password" placeholder="enter password" type="text" autocomplete="off" />
        <input type="hidden" name="_csrf" value="{{csrfToken}}">
    </div>
    <button id="login-button">login</button>
</form>

Not yet registered? Do it now! <a href="/register">Register</a>